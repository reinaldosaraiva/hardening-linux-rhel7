---
  - name: Registrar as vms rhel7 - Vmware
    shell: subscription-manager unregister && subscription-manager remove --all && subscription-manager clean && subscription-manager register --username {{ username_rh }} --password {{ password_rh }} && subscription-manager attach --pool {{ pool_id }} 
